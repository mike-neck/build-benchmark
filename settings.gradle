if (System.getenv('BUILD_CACHE') != null) {
  buildCache {
    local {
      directory = new File('.cache')
      removeUnusedEntriesAfterDays 20
    }
  }
}

rootProject.name = 'build-benchmark'
include('lib')
include "generator"
include 'with-interface'
include 'without-interface'
